
import { dateToYear } from 'frontend/helpers/date-to-year';
import { module, test } from 'qunit';

module('Unit | Helper | date to year');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateToYear([42]);
  assert.ok(result);
});


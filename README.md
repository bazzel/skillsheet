
## Get up and running

* `git clone <this repo>`
* `bundle install`
* `rails db:setup`
* `npm install`
* `npm -g install elm`
* `foreman start`
*  Open a browser and navigate to http://localhost:5000
